# 系统初始设计

## 一、系统定位

这是一个**面向软件研发任务的智能执行管理平台**。

它不模拟传统公司的层级组织，也不依赖大量固定的“前端 Agent、后端 Agent、测试 Agent”。平台的核心职责是：

> 根据任务内容、项目规则、能力要求、成本预算和安全级别，动态选择合适的 Agent 后端，并控制任务按照正确流程完成。

核心结构是：

```text
任务
  ↓
理解任务与识别风险
  ↓
装配项目上下文和执行准则
  ↓
选择合适的后端、模型、权限和预算
  ↓
Agent 自主执行
  ↓
测试、审查、人工审批
  ↓
合入或结束
```

---

## 二、核心思想

### 1. Agent 是动态执行实例，不是固定岗位

一个执行实例由以下内容组合而成：

```text
Agent 后端
+ 模型
+ 人格
+ 项目上下文
+ Skills
+ 工具和权限
+ 成本预算
+ 当前工作流阶段
```

同一个编程 Agent 后端可以处理前端、后端、测试或架构任务，领域能力主要来自动态加载的项目资料、准则和 Skill。

长期维护少量有真实差异的执行类型，例如：

* 经济型：处理简单、明确、低风险任务
* 标准型：处理大多数日常开发任务
* 专家型：处理复杂分析、疑难问题和大型重构
* 多模态型：处理页面、截图、视觉验证
* 可信型：处理敏感代码和受限数据
* 审查型：独立检查结果，不直接修改代码

---

### 2. 项目主控负责语义调度，平台负责确定性控制

每个项目显式指定一个主控角色。主控使用项目选定角色自己的固定 Runtime/模型，负责理解目标、拆解任务、创建任务频道或面板，并为其他角色编写任务简报；项目之间的主控和角色完全隔离。派发通过结构化的 Agent Tool 参数（`message.publish` 的 `mentions`）显式执行，不通过自然语言正文——Agent 消息里的 `@` 永远只是文字。

主控不是权限和质量门禁本身。安全、预算、工作流推进、证据核验和人工审批仍由规则系统确定性执行，不能由主控回复绕过。

Manager 主要决定：

```text
这个任务是什么
需要哪些上下文
需要什么能力
可以交给哪些后端
使用哪个成本等级
允许访问哪些资源
需要经过哪些测试和审批
```

具体如何搜索代码、拆分任务、调用子 Agent、编写和修改代码，则由选中的成熟 Agent 自主完成。

也就是：

```text
平台决定“在什么约束下执行”
Agent 决定“具体如何完成”
```

---

### 3. 项目准则是执行规则，不只是提示词

每个项目维护多篇完整准则文档；准则可以包含章节正文并引用版本化文档库中的文件。语义上至少覆盖三类核心准则：

**项目准则**

描述项目目标、范围、业务边界和关键概念。

**开发准则**

描述架构原则、代码要求、变更约束和兼容性要求。

**验证准则**

描述不同类型的变更必须完成哪些测试和提交哪些证据。

例如：

```text
前端界面变化
→ 必须完成浏览器流程测试和视觉验证

Bug 修复
→ 必须先复现，并提供修复前失败、修复后通过的回归证据

认证逻辑变化
→ 必须增加安全审查

生产环境变更
→ 必须经过人工审批
```

其中关键要求由平台强制执行，而不是仅依赖 Agent 自觉遵守。

---

### 4. 敏感资源不进入普通上下文

Token、数据库凭据、生产环境权限等敏感资源不能直接写入 Prompt、Skill 或任务描述。

Agent 获得的是受控能力，例如：

```text
读取指定范围的生产日志
调用测试支付接口
部署到预览环境
查询测试数据库
执行指定版本的生产发布
```

平台在实际工具调用时完成凭据注入、权限校验、日志脱敏和访问审计。

因此，后端是否能处理敏感任务，由确定性的安全策略决定，不能由 Agent 自己判断。

---

## 三、初始系统架构

```text
                         统一任务入口
                              │
                              ▼
┌────────────────────────────────────────────┐
│                控制平面                    │
│                                            │
│  任务理解       项目上下文装配              │
│  工作流选择     后端与模型路由              │
│  成本与配额     安全与权限策略              │
│  人工审批       质量门禁                    │
└───────────────────┬────────────────────────┘
                    │ 生成执行配置
                    ▼
┌────────────────────────────────────────────┐
│                执行平面                    │
│                                            │
│  Coding Agent / Research Agent / Reviewer  │
│  隔离工作区 / 浏览器 / 测试环境 / 工具      │
│  受控资源访问 / 临时权限                    │
└───────────────────┬────────────────────────┘
                    ▼
            测试证据、审查、合入、关闭
```

---

## 四、系统核心模块

### 1. 项目中心

管理：

* 项目简介、目标和范围
* 唯一主控角色及其固定 Runtime/模型
* 面向不同任务的频道
* 代码仓库和版本化文档库
* 完整的开发、测试、部署等准则文档
* 按 Runtime 过滤和注入的项目 Skills
* 内置任务看板与可动态布局的自定义面板
* 发布和审批规则

项目是领域知识和协作边界的主要载体；角色保存的是可配置身份与固定执行组合，实际 Agent 进程仍按消息或任务阶段临时启动。

### 2. 后端与模型中心

记录每个 Agent 后端和模型的：

* 编程和推理能力
* 多模态能力
* 网络搜索能力
* 是否支持内部多 Agent
* 成本和剩余额度
* 数据安全等级
* 可运行环境
* 当前可用状态

### 3. 智能路由器

该路由器只服务结构化任务的工作流阶段。聊天协作中的角色始终绑定固定 runtime/model;角色定位、能力与偏好用于协作选人,不会在每次 @ 执行时重新路由。

路由时依次考虑：

```text
安全限制
→ 必需能力
→ 项目和环境适配
→ 成功概率
→ 成本与剩余配额
```

简单任务优先使用低成本后端；任务失败、复杂度扩大或置信度过低时，再升级到更强模型。

### 4. 工作流引擎

根据任务类型选择流程。

需求流程：

```text
理解需求
→ 必要时人工澄清
→ 明确验收标准
→ 开发
→ 验证
→ 审查
→ 合入
```

Bug 流程：

```text
分析和复现
→ 确认有效
→ 根因分析
→ 修复
→ 回归测试
→ 审查
→ 合入
```

工作流控制阶段和门禁，不干涉 Agent 内部的具体执行方式。

### 5. 资源与安全中心

管理：

* 仓库权限
* 测试环境
* API 和 Token
* 数据库
* 部署能力
* 生产日志
* 敏感数据等级

资源授权必须是任务级、阶段级、限时和可审计的。

### 6. 证据与审批中心

保存并验证：

* 需求理解
* 澄清结果
* 实施计划
* Bug 复现证据
* 代码变更
* 测试报告
* 页面截图
* 浏览器测试结果
* Reviewer 结论
* 人工审批记录

任务是否完成，由可验证证据决定，而不是由 Agent 声称“已经完成”决定。

---

## 五、一个任务的基本执行方式

以 Bug 为例：

```text
用户提交 Bug
    ↓
平台识别项目、风险和任务类型
    ↓
加载项目准则、相关仓库和测试要求
    ↓
根据能力、成本和安全性选择执行后端
    ↓
Agent 先分析并尝试复现
    ↓
无法复现：请求补充信息
确认有效：进入修复阶段
    ↓
Agent 完成代码修改和回归测试
    ↓
平台检查测试证据是否满足项目准则
    ↓
根据风险自动审查或请求人工审批
    ↓
合入并关闭任务
```

在整个过程中，通常只有一个主要 Coding Agent 负责端到端执行。该 Agent 可以自行使用其原生多 Agent 能力。

平台只在以下情况下切换或增加执行者：

* 当前后端能力不足
* 需要不同安全等级的后端
* 需要特殊环境或权限
* 需要独立审查
* 需要多模态视觉验证
* 需要人工决策

---

## 六、推荐的初始版本

第一版不需要构建复杂的数字公司组织结构，只需要：

```text
一个统一任务入口
一个项目与准则系统
一个工作流引擎
一个后端和模型注册表
一个能力、成本、安全路由器
一个受控资源系统
一个测试证据与人工审批系统
```

长期 Agent 类型控制在少量几种：

```text
经济型执行者
标准执行者
专家执行者
可信环境执行者
多模态验证者
独立 Reviewer
```

最终形成的系统不是：

```text
总经理 → 部门经理 → 前端员工 → 测试员工
```

而是：

```text
任务
→ 策略与流程控制
→ 动态生成合适的执行配置
→ 一个成熟 Agent 端到端完成
→ 按风险验证和审批
```

这套设计的核心优势是：**执行保持扁平，管理由策略驱动，能力可以动态组合，成本可以优化，敏感资源可以严格隔离，研发过程可以被验证和审计。**
